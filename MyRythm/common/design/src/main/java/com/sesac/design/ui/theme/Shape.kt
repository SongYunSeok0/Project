package com.sesac.design.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

// 버튼, 카드 등 컴포넌트의 모서리 모양 정의. (Shapes 객체).

// 입력 필드, 메인 버튼에 사용되는 10dp 둥근 모서리
val AuthFieldShape = RoundedCornerShape(10.dp)

// 하단 링크 버튼('로그인' 버튼)에 사용되는 8dp 둥근 모서리
val AuthLinkButtonShape = RoundedCornerShape(8.dp)

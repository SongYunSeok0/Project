<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        [중요] 네이버 지도 SDK 호환성 설정:
        네이버 지도 SDK에 포함된 'LogoView' 등이 AppCompat 테마 기반
        테마 충돌 및 인증 오류 방지를 위해 앱의 기본 테마를 Theme.AppCompat으로 지정
    -->
    <style name="Theme.MyRythm" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- 여기에 색상 등 다른 테마 속성을 추가 가능 -->
    </style>

    <!--
        ====================================================
        기존 테마 (참고용으로 남겨둠)
        ====================================================
        <style name="Theme.MyRythm" parent="android:Theme.Material.Light.NoActionBar" />

        또는 Material3 테마를 사용했을 경우:
        <style name="Theme.MyRythm" parent="Theme.Material3.DayNight.NoActionBar">
            ...
        </style>
    -->

</resources>
